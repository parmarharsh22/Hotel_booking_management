<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LuxeStay - Room Categories</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 text-slate-800 min-h-screen flex flex-col">

    <div class="flex flex-1 flex-col md:flex-row">
        <!-- Sidebar Navigation -->
        <aside class="w-full md:w-64 bg-white border-r border-slate-200 flex flex-col justify-between p-4 shrink-0">
            <div>
                <div class="text-2xl font-bold text-[#0f172a] tracking-tight px-3 py-4">
                    LuxeStay
                </div>
                
              <nav class="mt-4 space-y-1">
                    <a href="/hotelAdmin/dashboard" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium text-slate-600 hover:bg-slate-100 transition">
                        <i class="fa-solid fa-table-columns w-5 text-slate-400"></i>
                        <span>Dashboard</span>
                    </a>
                    <a href="/hotelAdmin/rooms" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium bg-[#1e293b] text-white transition">
                        <i class="fa-solid fa-bed w-5"></i>
                        <span>Rooms</span>
                    </a>
                    <a href="/hotelAdmin/room-types/new" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium text-slate-600 hover:bg-slate-100 transition">
                        <i class="fa-solid fa-calendar-days w-5 text-slate-400"></i>
                        <span>Room Types</span>
                    </a>
                    <a href="hotelAdmin/staff" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium text-slate-600 hover:bg-slate-100 transition">
                        <i class="fa-solid fa-address-book w-5 text-slate-400"></i>
                        <span>Staff</span>
                    </a>
                    <a href="/hotelAdmin/policies/new" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium text-slate-600 hover:bg-slate-100 transition">
                        <i class="fa-solid fa-chart-simple w-5 text-slate-400"></i>
                        <span>Check-ins</span>
                    </a>
                    <a href="/hotelAdmin/settings" class="flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium text-slate-600 hover:bg-slate-100 transition">
                        <i class="fa-solid fa-gear w-5 text-slate-400"></i>
                        <span>Check-Outs</span>
                    </a>
                </nav>
            </div>

            <div class="mt-8 pt-4 border-t border-slate-100 space-y-1">
                <a href="#" class="flex items-center space-x-3 px-3 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100 rounded-lg transition">
                    <i class="fa-regular fa-circle-question w-5 text-slate-400"></i>
                    <span>Help Center</span>
                </a>
                <a href="" onclick="handleLogout()" class="flex items-center space-x-3 px-3 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100 rounded-lg transition">
                    <i class="fa-solid fa-arrow-right-from-bracket w-5 text-slate-400"></i>
                    <span>Logout</span>

<script>

function handleLogout() {
  
    // Clear all cookies
    document.cookie.split(";").forEach((cookie) => {
        const cookieName = cookie.split("=")[0].trim();

        document.cookie = `${cookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;
        document.cookie = `${cookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=${window.location.hostname};`;
    });

    // Clear localStorage and sessionStorage if used
    localStorage.clear();
    sessionStorage.clear();

    // Redirect to login page
    window.location.href = "/login";
}

</script>

                </a>
            </div>
        </aside>

        <!-- Main Workspace -->
        <main class="flex-1 flex flex-col">
            <header class="h-16 bg-white border-b border-slate-200 flex items-center justify-between px-6 md:px-8">
                <div>
                    <h1 class="text-xl font-bold text-slate-900">Room Categories</h1>
                    <p class="text-xs text-slate-500 hidden sm:block">Global Management System</p>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="flex items-center space-x-3 bg-slate-50 border border-slate-200 rounded-lg px-3 py-1.5 cursor-pointer hover:bg-slate-100 transition">
                        <div class="w-7 h-7 bg-slate-800 rounded-md flex items-center justify-center text-white text-xs font-bold">LS</div>
                        <div class="text-left hidden sm:block">
                            <p class="text-xs font-semibold text-slate-800 leading-tight">LuxeStay Signature</p>
                            <p class="text-[10px] text-slate-500 leading-none">London, UK</p>
                        </div>
                        <i class="fa-solid fa-chevron-down text-xs text-slate-400"></i>
                    </div>
                    <button class="p-2 text-slate-400 hover:text-slate-600 relative">
                        <i class="fa-regular fa-bell text-lg"></i>
                    </button>
                    <div class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center text-white text-sm font-medium cursor-pointer">
                        A
                    </div>
                </div>
            </header>

            <div class="p-6 md:p-8 space-y-6 max-w-[1600px] w-full mx-auto">
                
                <!-- Controls and Actions Header -->
             

                <!-- KPI Metric Row -->
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                    <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                        <p class="text-xs font-medium text-slate-400 uppercase tracking-wider">Total Categories</p>
                        <div class="flex items-baseline space-x-2 mt-2">
                            <span class="text-2xl font-bold text-slate-900">12</span>
                            <span class="text-xs font-semibold text-emerald-600 bg-emerald-50 px-1.5 py-0.5 rounded">+1 New</span>
                        </div>
                    </div>
                    <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                        <p class="text-xs font-medium text-slate-400 uppercase tracking-wider">Avg. Base Price</p>
                        <div class="flex items-baseline space-x-2 mt-2">
                            <span class="text-2xl font-bold text-slate-900">$420</span>
                            <span class="text-xs font-medium text-amber-600 bg-amber-50 px-1.5 py-0.5 rounded">Peak Season</span>
                        </div>
                    </div>
                    <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                        <p class="text-xs font-medium text-slate-400 uppercase tracking-wider">Active Inventory</p>
                        <div class="flex items-baseline space-x-2 mt-2">
                            <span class="text-2xl font-bold text-slate-900">184</span>
                            <span class="text-xs text-slate-500">Rooms Total</span>
                        </div>
                    </div>
                    <div class="bg-[#0f172a] p-5 rounded-xl border border-slate-800 shadow-sm text-white">
                        <p class="text-xs font-medium text-slate-400 uppercase tracking-wider">RevPAR Forecast</p>
                        <div class="flex items-baseline space-x-2 mt-2">
                            <span class="text-2xl font-bold text-white">$312.45</span>
                            <span class="text-xs text-slate-400"><i class="fa-solid fa-chart-line"></i></span>
                        </div>
                    </div>
                </div>

                <!-- Inventory Data Sheet -->
                <div class="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
    <div class="p-4 border-b border-slate-200 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3">
        <div>
            <h3 class="font-bold text-slate-900">Modify Property Room Configuration</h3>
            <p class="text-xs text-slate-500">Update specific room definitions, pricing maps, and live status conditions.</p>
        </div>
        <span class="text-[10px] px-2 py-0.5 rounded bg-slate-100 text-slate-700 font-semibold border border-slate-200 uppercase tracking-wider">
            Room ID: <%= room.room_id %>
        </span>
    </div>

    <form id="editRoomForm" onsubmit="handleFormSubmission(event, '<%= room.room_id %>')" class="p-6 md:p-8 space-y-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            
            <div class="space-y-4">
                <div>
    <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1.5">
        Room Number
    </label>
<div class="relative">
                        <input
        type="text"
        id="roomNumber"
        name="room_number"
        value="<%= room.room_number %>"
        onblur="validateRoomNumberUniqueness(this.value)"
        data-original-room-number="<%= room.room_number %>"
        required
        class="w-full text-sm bg-slate-50 border border-slate-200 rounded-md p-2.5 outline-none focus:bg-white focus:border-slate-400 font-medium text-slate-800 transition"
    >
                        <div id="validationFeedbackIcon" class="absolute right-3 top-1/2 -translate-y-1/2 hidden text-xs"></div>
                    </div>
                    <p id="validationFeedbackText" class="text-[11px] mt-1 transition-all hidden"></p>
    

   <p id="validationFeedbackText" class="text-[11px] mt-1 transition-all hidden"></p>
</div>

                <div>
                    <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1.5">Floor Level</label>
                    <input type="number" name="floor" value="<%= room.floor %>" required
                        class="w-full text-sm bg-slate-50 border border-slate-200 rounded-md p-2.5 outline-none focus:bg-white focus:border-slate-400 font-medium text-slate-800 transition">
                </div>

                <div id="type_container">
                  <%- include('../partials/components/TypeDropDown', {
    id: 'roomTypeDropdown',
    name: 'room_type_id',
    label: 'Room Category Allocation',
    required: true,
    selectedValue: room.room_type_id
}) %>

                </div>


                 <%- include('../partials/components/RoomStatusDropDown', {
    id: 'roomStatusDropdown',
    name: 'room_status_id',
    label: 'Room Status',
    required: true,
    selectedValue: room.room_status_id
}) %>
<script>

document.addEventListener('DOMContentLoaded', () => {

    DynamicDropdown.load({
        elementId: 'roomStatusDropdown',
        endpoint: '/hotelAdmin/room-statuses',
        valueKey: 'room_status_id',

        labelBuilder: (status) =>
            status.status_name
    });

});

</script>
               

            <div class="space-y-4 flex flex-col justify-between">
                <div>
                    <label class="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-1.5">Internal Operations Notes</label>
                    <textarea name="notes" rows="4" placeholder="Housekeeping conditions, hardware leaks or client profiles changes..."
                        class="w-full text-sm bg-slate-50 border border-slate-200 rounded-md p-2.5 h-[114px] outline-none focus:bg-white focus:border-slate-400 font-medium text-slate-800 transition resize-none"><%= room.notes %></textarea>
                </div>

                <div>
                   
                </div>
            </div>
        </div>

        <div class="flex items-center justify-end gap-3 pt-4 border-t border-slate-100">
            <button type="button" onclick="resetFormState()" 
                class="flex items-center space-x-2 text-xs text-slate-500 hover:text-slate-800 font-medium px-4 py-2.5 rounded transition">
                <i class="fa-solid fa-arrow-rotate-left text-[10px]"></i>
                <span>Reset Modifications</span>
            </button>
            <button type="submit" id="submitButton"
                class="flex items-center space-x-2 bg-[#0f172a] hover:bg-slate-800 text-white text-xs font-medium px-5 py-2.5 rounded-md shadow-sm transition">
                <i class="fa-regular fa-floppy-disk text-[11px]"></i>
                <span>Save Parameters</span>
            </button>
        </div>
    </form>
</div>

<script>

 let isRoomNumberValid = false;

    /**
     * Triggers asynchronous pipeline check to avoid database level composite duplicate constraint rejections
     */
    async function validateRoomNumberUniqueness(roomNumber) {
       const roomInput =
        document.getElementById("roomNumber");

    const originalRoomNumber =
        roomInput.dataset.originalRoomNumber;

    // Skip validation if unchanged
    if (roomNumber.trim() === originalRoomNumber.trim()) {

        isRoomNumberValid = true;

        document
            .getElementById("submitButton")
            .disabled = false;

        return;
    }

        const textFeedback = document.getElementById('validationFeedbackText');
        const iconFeedback = document.getElementById('validationFeedbackIcon');
        const submitBtn = document.getElementById('submitButton');
            // Matches our input architecture configuration context

        // Setup loading state indicators
        iconFeedback.className = "absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 animate-spin";
        iconFeedback.innerHTML = `<i class="fa-solid fa-spinner"></i>`;
        iconFeedback.classList.remove('hidden');

        try {
            // Adjust this API string path to perfectly match your verification endpoint
            const checkUrl = `/hotelAdmin/rooms/verifyRoomExists?room_number=${encodeURIComponent(roomNumber)}`;
            const response = await fetch(checkUrl);
            const data = await response.json();

            textFeedback.classList.remove('hidden');

            if (data.exists) {
                // Duplicate identity conflict matches found
                isRoomNumberValid = false;
            
                submitBtn.disabled = true;
                textFeedback.className = "text-[11px] mt-1 text-rose-600 font-medium";
                textFeedback.innerText = "✕ Identification mismatch: This room identifier actively exists in this property asset.";
                iconFeedback.className = "absolute right-3 top-1/2 -translate-y-1/2 text-rose-500";
                iconFeedback.innerHTML = `<i class="fa-solid fa-circle-xmark"></i>`;
            } else {
                // Identity clean to proceed
                isRoomNumberValid = true;
                submitBtn.disabled = false;
                textFeedback.className = "text-[11px] mt-1 text-emerald-600 font-medium";
                textFeedback.innerText = "✓ Room configuration identifier code verified unique.";
                iconFeedback.className = "absolute right-3 top-1/2 -translate-y-1/2 text-emerald-500";
                iconFeedback.innerHTML = `<i class="fa-solid fa-circle-check"></i>`;
            }
        } catch (err) {
            console.error("Uniqueness engine connection interruption context: ", err);
            // Graceful fallback to avoid locking administration interface due to temporary server lag
            iconFeedback.classList.add('hidden');
            textFeedback.classList.add('hidden');
            submitBtn.disabled = false;
            isRoomNumberValid = true;
        }
    }






    /**
     * Intercepts file select events to safely push blob streams 
     * directly into the image element interface for real-time validation.
     */
    function previewSelectedPhoto(inputElement) {
        const previewImg = document.getElementById('roomPhotoPreview');
        const selectedFile = inputElement.files[0];

        if (selectedFile) {
            const objectUrlStream = URL.createObjectURL(selectedFile);
            previewImg.src = objectUrlStream;

            // Optional: Free up performance garbage collections once object finishes drawing
            previewImg.onload = function() {
                URL.revokeObjectURL(previewImg.src); 
            }
        }
    }
let roomNumberTimeout;

async function VerifyRoomNumber(event) {

    const input = event.target;
    const roomNumber = input.value.trim();

    const errorElement = document.getElementById("roomNumberError");

    // reset state
    input.classList.remove(
        "border-red-500",
        "focus:border-red-500"
    );

    errorElement.classList.add("hidden");

    if (!roomNumber) return;

    // Debounce API calls
    clearTimeout(roomNumberTimeout);

    roomNumberTimeout = setTimeout(async () => {

        try {

            const response = await fetch(
    '/hotelAdmin/rooms/verifyRoomExists',
    {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            room_number: roomNumber
        })
    }
);
const data = await response.json();
            if (data.exists) {

                input.classList.remove("border-slate-200");

                input.classList.add(
                    "border-red-500",
                    "focus:border-red-500"
                );

                errorElement.classList.remove("hidden");

            } else {

                input.classList.remove(
                    "border-red-500",
                    "focus:border-red-500"
                );

                errorElement.classList.add("hidden");
            }

        } catch (error) {

            console.error(
                "Room number validation failed:",
                error
            );

        }

    }, 500); // wait 500ms after user stops typing
}


    /**
     * Resets input nodes back to original data properties and restores base asset frames
     */
    function resetFormState() {
        const structuralForm = document.getElementById('editRoomForm');
        const previewImg = document.getElementById('roomPhotoPreview');
        
        structuralForm.reset();
        // Restore pristine image context from default tracker parameter
        previewImg.src = previewImg.getAttribute('data-default-src');
    }

    /**
     * Asynchronous fetch request router targeting your administrative put pipeline.
     */
    async function handleFormSubmission(event, roomId) {
        event.preventDefault();
        const targetedForm = event.target;
        
        // Packaging components inside a FormData engine to process active file buffers cleanly
        const transmissionPayload = new FormData(targetedForm);
        console.log(transmissionPayload);
        
        try {
            const apiResultResponse = await fetch(`/hotelAdmin/rooms/${roomId}`, {
                method: 'PUT',
                body: transmissionPayload
            });

            if (apiResultResponse.status ===200) {
                alert('Room specifications configuration applied successfully.');
                // Route user context back to tracking views or refresh data layout maps
                window.location.reload();
                
            } else {
                const faultData = await apiResultResponse.json();
                alert(`Configuration Exception Encountered: ${faultData.message || 'System fault validation failed.'}`);
            }
        } catch (networkFault) {
            console.error('API Pipeline Breakdown Context: ', networkFault);
            alert('A network exception interrupted data synchronization operations.');
        }
    }
</script>

                    <!-- Pagination / Footer Indicators -->
                    <div class="p-4 border-t border-slate-200 flex items-center justify-between flex-wrap gap-2 text-xs text-slate-500">
                        
                        <div class="flex items-center space-x-1">
                        </div>
                    </div>
                </div>

            </div>

            <!-- Page Footer -->
            <footer class="mt-auto bg-white border-t border-slate-200 py-6 px-6 md:px-8 flex flex-col md:flex-row items-center justify-between text-xs text-slate-500 gap-4">
                <div>
                    <span class="font-semibold text-slate-700">LuxeStay</span> &copy; 2026 LuxeStay Hospitality Group. All rights reserved.
                </div>
                <div class="flex items-center space-x-4">
                    <a href="#" class="hover:text-slate-800 transition">Privacy Policy</a>
                    <a href="#" class="hover:text-slate-800 transition">Terms of Service</a>
                    <a href="#" class="hover:text-slate-800 transition">Cookie Policy</a>
                    <a href="#" class="hover:text-slate-800 transition">Accessibility</a>
                </div>
            </footer>
        </main>
    </div>

    <!-- Interface Event Control Scripts -->
    <script>
        // Safely parse the EJS variables directly into a client-side database matrix 
        // to keep search/filter features working completely without extra API hits!
        // let mockRoomsDatabase = <%- JSON.stringify(room) %>;
        // console.log(mockRoomsDatabase);
        
        // Toggle Filter UI Popup Card
        function toggleFilterDropdown() {
            const dropdown = document.getElementById('filterDropdown');
            dropdown.classList.toggle('hidden');
        }

        // Close dropdown when clicking outside components boundary
        window.addEventListener('click', function(e) {
            const dropdown = document.getElementById('filterDropdown');
            if (!dropdown.contains(e.target) && !e.target.closest('button')?.innerText.includes('Filter')) {
                dropdown.classList.add('hidden');
            }
        });

        // Dynamic table structural redraw client side for sorting/filtering mechanisms
        function renderRoomCards(dataset) {
            const container = document.getElementById('roomContainer');
            container.innerHTML = '';

            if (dataset.length === 0) {
                container.innerHTML = `
                    <tr>
                        <td colspan="5" class="py-8 text-center text-slate-400 font-medium">
                            No active entries match current filter rules.
                        </td>
                    </tr>`;
                updateCounters(0);
                return;
            }

            dataset.forEach(room => {
                let statusBadge = room.room_status_id === 1 
                    ? '<span class="text-[10px] px-2 py-0.5 rounded bg-emerald-50 text-emerald-700 font-semibold border border-emerald-200">Available</span>'
                    : '<span class="text-[10px] px-2 py-0.5 rounded bg-amber-50 text-amber-700 font-semibold border border-amber-200">Occupied</span>';

                let amenitiesHtml = (room.amenities || []).map(icon => 
                    `<i class="fa-solid ${icon} text-xs text-slate-400 bg-slate-100 p-1 rounded"></i>`
                ).join(' ');

                let imgUrl = room.photo_url || 'https://images.unsplash.com/photo-1618773928121-c32242e63f39?auto=format&fit=crop&w=200&q=80';

                let rowHtml = `
                    <tr class="hover:bg-slate-50/70 transition">
                        <td class="py-4 px-6 flex items-center space-x-4">
                            <img src="${imgUrl}" alt="Room Thumbnail" class="w-12 h-12 object-cover rounded-lg bg-slate-100 shrink-0 border border-slate-200">
                            <div>
                                <div class="font-semibold text-slate-900">${room.category_name}</div>
                                <div class="text-[10px] tracking-wide font-bold text-slate-400 uppercase mt-0.5">${room.tag || ''}</div>
                            </div>
                        </td>
                        <td class="py-4 px-6">
                            <div class="text-xs font-semibold text-slate-700">Room ${room.room_number}</div>
                            <div class="text-[11px] text-slate-400">Floor Layout: ${room.floor}</div>
                        </td>
                        <td class="py-4 px-6 max-w-xs">
                            <div class="flex items-center space-x-2 mb-1.5">
                                ${amenitiesHtml}
                                ${statusBadge}
                            </div>
                            <p class="text-[11px] text-slate-500 italic truncate" title="${room.notes || ''}">"${room.notes || 'No extra notes.'}"</p>
                        </td>
                        <td class="py-4 px-6 font-bold text-slate-900 text-base">
                            ${room}
                        </td>
                        <td class="py-4 px-6 text-right">
                            <div class="flex items-center justify-end space-x-2 text-slate-400">
                                <button class="p-1.5 hover:text-slate-600 hover:bg-slate-100 rounded transition"><i class="fa-regular fa-eye"></i></button>
                                <button class="p-1.5 hover:text-slate-600 hover:bg-slate-100 rounded transition"><i class="fa-regular fa-pen-to-square"></i></button>
                            </div>
                        </td>
                    </tr>
                `;
                container.insertAdjacentHTML('beforeend', rowHtml);
            });

            updateCounters(dataset.length);
        }

        // Reactive UI text adjustments for table metrics counters
        function updateCounters(count) {
            document.getElementById('visibleCounter').innerText = `${count} type${count !== 1 ? 's' : ''} visible`;
            document.getElementById('showingIndicator').innerText = `Showing 1-${count} of ${count} items`;
        }

        // Filter Evaluation Engine 
        function applyFilters() {
            const typeValue = document.getElementById('filterRoomType').value;
            const statusValue = document.getElementById('filterStatus').value;
            const floorValue = document.getElementById('filterFloor').value;

            let filteredResult = mockRoomsDatabase.filter(room => {
                let matchesType = typeValue === "" || room.room_type_id == typeValue;
                let matchesStatus = statusValue === "" || room.room_status_id == statusValue;
                let matchesFloor = floorValue === "" || room.floor == floorValue;
                return matchesType && matchesStatus && matchesFloor;
            });

            renderRoomCards(filteredResult);
            toggleFilterDropdown(); 
        }

        // Clear filter panel state triggers
        function resetFilters() {
            document.getElementById('filterRoomType').value = "";
            document.getElementById('filterStatus').value = "";
            document.getElementById('filterFloor').value = "";
            renderRoomCards(mockRoomsDatabase);
            toggleFilterDropdown();
        }

        // Live client-side interactive search engine
        function searchTable() {
            const searchVal = document.getElementById('tableSearch').value.toLowerCase();
            let queryMatch = mockRoomsDatabase.filter(room => {
                return (room.category_name || '').toLowerCase().includes(searchVal) || 
                       (room.notes || '').toLowerCase().includes(searchVal) ||
                       (room.room_number || '').toString().toLowerCase().includes(searchVal);
            });
            renderRoomCards(queryMatch);
        }
    </script>
</body>
</html>